---
- hosts: all
  become: true
  gather_facts: false
  tasks:
    - name: update apt cache
      apt: update_cache=yes cache_valid_time=86400
      tags: [ 'packages' ]

  roles: 
    - common

- include: php.yml
- include: wkhtmltopdf.yml
- include: nodejs.yml
- include: nginx.yml
