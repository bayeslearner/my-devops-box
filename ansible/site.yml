---
- hosts: api
  become: true
  gather_facts: true
  debugger: on_failed
  tasks:
    - name: update apt cache
      apt: update_cache=yes cache_valid_time=86400
      tags: [ 'packages' ]

  roles:
    - php
